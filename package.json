{
  "name": "kimp-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "dev:worker": "wrangler dev workers/price-api/src/index.ts --config workers/price-api/wrangler.toml --port 8787",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.10",
    "astro": "^6.1.8"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
